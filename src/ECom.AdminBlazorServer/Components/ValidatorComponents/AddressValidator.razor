@inherits BaseValidatorComponent

<RadzenLengthValidator Max="@ValidationSettings.MaxNameLength"
                       Min="@ValidationSettings.MinNameLength"
                       Popup="@Popup"
                       Culture="@CurrentCultureInfo"
                       Style="@Style"
                       Text="@Loc["length_must_be_in_between"].Format("name", Loc["address"]).Format("max", ValidationSettings.MaxNameLength).Format("min", ValidationSettings.MinNameLength)"
                       Component="Component">
</RadzenLengthValidator>

@code {

}
