@inherits BaseValidatorComponent

<RadzenLengthValidator Component="@Component" 
                       Popup="@Popup"
                       Culture="@CurrentCultureInfo"
                       Style="@Style"
                       Max="@StringValidation.Max" 
                       Min="@StringValidation.Min" 
                       Text="@Loc["must_be_in_range"].Format("name", Loc[LocKey]).Format("max", StringValidation.Max).Format("min", StringValidation.Min)"></RadzenLengthValidator>


@code {

}
