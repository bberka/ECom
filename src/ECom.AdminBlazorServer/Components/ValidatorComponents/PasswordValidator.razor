@inherits BaseValidatorComponent

<RadzenLengthValidator Component="@Component" 
                       Popup="@Popup"
                       Culture="@CurrentCultureInfo"
                       Style="@Style"
                       Max="@ValidationSettings.MaxPasswordLength" 
                       Min="@ValidationSettings.MinPasswordLength" 
                       Text="@Loc["must_be_in_range"].Format("name", Loc["password"]).Format("max", ValidationSettings.MaxPasswordLength).Format("min", ValidationSettings.MinPasswordLength)"></RadzenLengthValidator>

@code {

}
